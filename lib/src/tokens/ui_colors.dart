import 'package:flutter/widgets.dart';

/// Unified dynamic color tokens for the custom Material Wallet design system.
/// Matches the colors in the mockup guidelines.
class UIColors {
  UIColors._();

  /// Primary Vishnu Cosmic Blue (his skin color).
  static const Color primaryBlue = Color(0xFF0F2C59);

  /// Pitambar Gold theme color.
  static const Color pitambarGold = Color(0xFFD4AF37);

  /// Lotus Rose accent/error color.
  static const Color lotusRose = Color(0xFFF28F8F);

  /// Main screen background color (serene sand off-white).
  static const Color systemBackground = Color(0xFFFAF8F5);

  /// Card/sheet background color (solid white).
  static const Color cardBackground = Color(0xFFFFFFFF);

  /// Primary text color (charcoal dark).
  static const Color textPrimary = Color(0xFF1A1A1A);

  /// Secondary text color (medium grey).
  static const Color textSecondary = Color(0xFF7A7A7A);

  /// Thin divider line color.
  static const Color separator = Color(0xFFEDEDED);
}
